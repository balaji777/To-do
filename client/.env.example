VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional: absolute URL of the deployed API (e.g. https://your-server.onrender.com/api).
# Leave unset for local dev, where the Vite dev server proxies /api to localhost:4000.
VITE_API_URL=
